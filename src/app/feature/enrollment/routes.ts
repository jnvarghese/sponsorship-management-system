import { NgModule } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';
import { EnrollmentComponent } from './index';

export const enrollmentRoutes: Routes = [  
    { path: 'enroll', component: EnrollmentComponent },  
  ];

